# Supabase — server-side (private, not exposed to browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase — client-side (exposed to browser via PUBLIC_ prefix)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
